#include "main.h"
/**
  * main - testing function that prints if an integer is positive or negative
  * Return: 0 (Success)
  */
int main(void)
{
	int p = 0;
       	positive_or_negative(p);
	return (0);
}
