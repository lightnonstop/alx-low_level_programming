#include <stdlib.h>
#include <stdio.h>
#include "main.h"
#include <string.h>
/**
  * _atoi - Converts an integer to a string.
  * @s: First Operand
  * Return: Integer (Success) or 0 (Failure)
  */
int _atoi(char *s)
{
	int new;

	new = atoi(s);
	return (new);
}
