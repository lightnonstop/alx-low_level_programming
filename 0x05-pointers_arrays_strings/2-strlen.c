#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
/**
  * _strlen - Computes the length of a string
  * @s: First Operand
  * Return: Length of a string
  */
int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
